import 'package:flutter/material.dart';

class ComidaScreen extends StatelessWidget {
  const ComidaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Comida'));
  }
}